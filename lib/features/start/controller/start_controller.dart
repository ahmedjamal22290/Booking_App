import 'dart:developer';

import 'package:get/get.dart';

class StartController extends GetxController {
  int page = 0;
  void animate() {
    // if (page == 2) {
    // log(page.toString());
    update();
    // }
  }
}
